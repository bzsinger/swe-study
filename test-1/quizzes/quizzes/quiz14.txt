What's the output of the following?
def test1 () :
    a = ([2], (3,), [4])
    for v in a :
        v += (5,)
    print(a)


All primary keys are super keys.
All super keys are primary keys.
[DD: Basic UML & SQL]

Which of the following is true?
[DD: Basic UML & SQL]
substitute keys have some descriptive value (e.g. CA, TX, etc.)
surrogate keys have some descriptive value (e.g. CA, TX, etc.) 
